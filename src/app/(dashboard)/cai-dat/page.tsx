import { Settings } from 'lucide-react';
import { SectionPage } from '@/components/layout/section-page';

export default function SettingsPage() {
  return <SectionPage title="Cài đặt" description="Quản lý tùy chọn tài khoản, thông báo và cấu hình không gian làm việc." icon={Settings} />;
}

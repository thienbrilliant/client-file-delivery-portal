import Link from 'next/link';import {ArrowLeft} from 'lucide-react';import {auth} from '../../../../../auth';import {prisma} from '@/lib/db/prisma';import {redirect,notFound} from 'next/navigation';
export default async function CustomerDetail({params}:{params:Promise<{id:string}>}){const session=await auth();if(!session?.user||session.user.role!=='ADMIN')redirect('/khach-hang');const {id}=await params;const customer=await prisma.user.findFirst({where:{id,role:'CUSTOMER'},include:{customerProfile:true,projectsOwned:{orderBy:{updatedAt:'desc'},include:{_count:{select:{files:true,folders:true}}}}}});if(!customer)notFound();const fileCount=customer.projectsOwned.reduce((sum,p)=>sum+p._count.files,0);return <div className="space-y-6"><Link href="/khach-hang" className="inline-flex items-center gap-2 text-sm text-[var(--muted)] hover:text-[var(--foreground)]"><ArrowLeft className="h-4 w-4"/>Khách hàng</Link><div><h1 className="text-2xl font-semibold tracking-tight">{customer.name||customer.email}</h1><p className="mt-1 text-sm text-[var(--muted)]">{customer.email}</p></div><div className="grid gap-3 sm:grid-cols-3"><div className="surface rounded-[10px] p-5"><p className="text-sm text-[var(--muted)]">Dự án</p><p className="mt-2 text-2xl font-semibold">{customer.projectsOwned.length}</p></div><div className="surface rounded-[10px] p-5"><p className="text-sm text-[var(--muted)]">File</p><p className="mt-2 text-2xl font-semibold">{fileCount}</p></div><div className="surface rounded-[10px] p-5"><p className="text-sm text-[var(--muted)]">Công ty</p><p className="mt-2 text-sm font-medium">{customer.customerProfile?.companyName||'—'}</p></div></div><div className="surface rounded-[10px] p-5"><h2 className="font-semibold">Dự án</h2><div className="mt-4 space-y-2">{customer.projectsOwned.length?customer.projectsOwned.map(p=><Link key={p.id} href={`/du-an/${p.id}`} className="flex items-center rounded-[8px] border border-[var(--border)] px-4 py-3 hover:bg-[var(--surface-muted)]"><span className="font-medium">{p.name}</span><span className="ml-auto text-xs text-[var(--muted)]">{p._count.files} file · {p.status}</span></Link>):<p className="py-8 text-center text-sm text-[var(--muted)]">Khách hàng này chưa có dự án nào.</p>}</div></div></div>}

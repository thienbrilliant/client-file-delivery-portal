import { History } from 'lucide-react';
import { SectionPage } from '@/components/layout/section-page';

export default function ActivityPage() {
  return <SectionPage title="Nhật ký hoạt động" description="Theo dõi lịch sử thao tác và các sự kiện quan trọng trong hệ thống." icon={History} />;
}
